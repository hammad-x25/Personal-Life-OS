import { verifyAccess } from '../services/token.service.js'; import User from '../models/User.js'; import { AppError, asyncHandler } from '../utils/api.js';
export const authenticate = asyncHandler(async (req, res, next) => { const token = req.cookies.accessToken || req.headers.authorization?.replace('Bearer ', ''); if (!token) throw new AppError('Authentication required', 401, 'UNAUTHENTICATED'); const payload = verifyAccess(token), user = await User.findById(payload.userId); if (!user) throw new AppError('User not found', 401, 'UNAUTHENTICATED'); req.user = user; next(); });
