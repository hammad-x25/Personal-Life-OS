import { connectDatabase } from './db.js'; import User from './models/User.js'; import Task from './models/Task.js'; import Goal from './models/Goal.js'; import Expense from './models/Expense.js'; import bcrypt from 'bcryptjs';
await connectDatabase(); const email = 'demo@lifeos.local'; let user = await User.findOne({ email }); if (!user) user = await User.create({ name: 'Demo User', email, passwordHash: await bcrypt.hash('password123', 12), registeredDateKey: new Date().toISOString().slice(0, 10) }); await Task.deleteMany({ userId: user._id }); await Goal.deleteMany({ userId: user._id }); await Expense.deleteMany({ userId: user._id }); await Task.insertMany([{ userId: user._id, title: 'Plan the day', status: 'TODO', priority: 'HIGH' }, { userId: user._id, title: 'Review backend architecture', status: 'IN_PROGRESS', priority: 'MEDIUM' }]); await Goal.create({ userId: user._id, title: 'Build Personal Life OS', target: 100, currentProgress: 15, unit: '%', priority: 'HIGH' }); await Expense.create({ userId: user._id, amount: 450, category: 'Food', description: 'Demo lunch', dateKey: new Date().toISOString().slice(0, 10), type: 'EXPENSE' }); console.log('Seeded demo@lifeos.local / password123'); process.exit(0);
